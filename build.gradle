allprojects {
    group 'com.team871'

    repositories {
        mavenCentral()

        flatDir {
         dirs "$rootDir/lib"
        }
    }

    apply plugin: 'idea'
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'idea'
    version = '1.0-SNAPSHOT'
    sourceCompatibility = 1.8

}